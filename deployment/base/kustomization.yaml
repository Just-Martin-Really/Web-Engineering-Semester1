# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Generates the `app-image` OCIArtifactTag provider consumed by the
  # deployment ResourceSet below; must precede it so the image reference
  # resolves before the Deployment is rendered.
  - image-provider.yaml
  - resourceset.yaml
