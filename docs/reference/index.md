# Reference

The exact facts about Kursforum: the HTTP API, every environment variable, the database schema, and the Kubernetes platform handoff. Values here are read from the source, not summarized from memory.

Browse the reference in the sidebar.
