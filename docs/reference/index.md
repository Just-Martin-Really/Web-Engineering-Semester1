# Reference

The exact facts about Kursforum: the HTTP API, every environment variable, and the database schema. Values here are read from the source, not summarized from memory.

Browse the reference in the sidebar.
